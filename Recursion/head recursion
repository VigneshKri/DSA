#include <iostream>
using namespace std;

void count(int n)
{
    if(n==0)
    {
        return ;
    }
    else
    {
       
        count(n-1);
         cout<<n<<" ";
    }
}
int main() {
   int n;
   cout<<"enter the number"<<endl;
   cin>>n;
   count(n);
    return 0;
}
